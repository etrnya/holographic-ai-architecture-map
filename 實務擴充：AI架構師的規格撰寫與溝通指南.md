# 核心實務擴充：AI 架構師的規格撰寫與溝通指南

## 適用對象
本指南適用於計畫使用 AI 工具（如 Cursor、Gemini、Claude）來輔助撰寫程式碼，期望將「開發時間」大幅縮短，並從「寫程式的打字員」轉型為「系統架構師」的實踐者。

## 核心觀念：從「發包工程」看 AI 協作

許多非技術人員在使用 AI 寫程式時常犯的錯誤，是給予過於模糊的指令（例如：「幫我寫一個可以上傳檢驗報告的網站」）。這就像是對著工頭說「幫我蓋一棟房子」，最終蓋出來的格局、管線與建材，絕對不會符合預期，甚至無法安全居住。

在 AI 驅動開發的時代，**AI 是那位不知疲倦的資深工程師與工頭，而發出指令的人，必須擔當「建築師」的角色。** 建築師不需要親自砌磚，但必須給出極度精確的「藍圖（規格書）」。

---

## 藍圖定義：軟體規格書 (PRD) 的黃金框架

在請 AI 寫下第一行程式碼之前，必須先在純文字檔或 Notion 中，清楚定義以下四大板塊。這份文件就是系統的「規格書 (Product Requirements Document, PRD)」。

### 1. 使用者故事 (User Story) 與核心流程
釐清系統的使用情境，讓 AI 理解商業邏輯。
*   **句型範本**：「作為一個 [什麼角色]，我想要 [執行什麼操作]，以便於 [達成什麼業務目標]。」
*   **範例**：「作為一名『醫檢師』，我想要『在系統中上傳一份 PDF 格式的 COA 報告』，以便於『系統能自動進行數值比對』。」

### 2. 資料庫綱要 (Database Schema)
資料庫是系統的地基，地基不穩，後面的程式碼全都是違章建築。必須明確告訴 AI，資料要如何儲存。
*   **撰寫重點**：定義出表格名稱、欄位名稱、資料型態。
*   **範例**：
    *   表格：`coa_reports` (儲存檢驗報告)
    *   欄位：`id` (唯一識別碼)、`upload_user_id` (上傳者編號)、`report_date` (報告日期)、`status` (狀態：待處理/異常/合格)。

### 3. API 規格 (前後端溝通協議)
定義外場（前端網頁）與廚房（後端系統）的標準溝通格式。
*   **撰寫重點**：定義傳送過去的資料（Request）與預期回傳的資料（Response）。
*   **範例**：
    *   **前端發送**：帶有圖片檔案與檢驗批號的表單資料。
    *   **後端回傳**：JSON 格式，包含 `{"success": true, "message": "辨識成功", "extracted_data": {...}}`。

### 4. 邊界條件與例外處理 (Edge Cases)
這是區分「玩具」與「企業級系統」的關鍵。系統不能只在一切順利時運作正常。
*   **撰寫重點**：列出可能發生的錯誤情況，並規定系統該如何反應。
*   **範例**：
    *   如果使用者上傳的檔案大於 10MB，系統必須回傳「檔案過大請壓縮」。
    *   如果 OCR 辨識找不到指定的化學成分，必須標示狀態為「人工介入處理」，不能讓系統直接崩潰。

---

## 實戰技巧：與 AI 協作的最佳提示詞 (Prompting) 策略

當準備好上述的規格書後，可以套用以下策略來指派任務給 AI：

### 技巧一：賦予角色與技術棧約束
不要讓 AI 盲目瞎猜。在對話的第一句，直接錨定其身分與技術框架（這與前幾階段的技術決策地圖互相呼應）。
> **Prompt 範例**：「你現在是一位具備 10 年經驗的資深全端工程師，深諳醫療與檢驗系統的資安規範。我們接下來要開發一套自動化系統。前端使用 React，後端使用 Python (FastAPI)，資料庫為 PostgreSQL。請在所有程式碼中遵循 OWASP 安全準則，特別留意權限驗證與防範 SQL Injection。」

### 技巧二：先要計畫，再要程式碼 (Plan First, Code Later)
要求 AI 一次吐出所有程式碼是災難的開始。應採用循序漸進的開發策略。
> **Prompt 範例**：「這是我寫的系統規格書（貼上 PRD）。請先不要寫任何程式碼。請先列出你打算怎麼實作的『步驟計畫 (Implementation Plan)』與『資料夾結構』。等我確認計畫無誤後，我們再一步一步開始寫程式。」

### 技巧三：限縮修改範圍與除錯
當程式發生錯誤時，不要只貼上錯誤訊息就讓 AI 隨意改寫。
> **Prompt 範例**：「在執行 `upload_report` 這個 API 時，發生了這個錯誤訊息：(貼上錯誤)。請先分析可能的原因，並列出 2-3 種解決方案。在我選擇方案之前，不要直接改寫完整的程式碼。」

---

## 職場洞察與反思

掌握了架構師的思維與 Prompting 技巧，便能將建立的 `AI-Driven-Project-Workflow` 發揮到極致。

在這個流程中，**人類的價值在於「定義問題的精確度」與「對最終品質的責任感」**。程式碼只是解決問題的產物，只要規格書寫得足夠嚴謹、測試條件抓得足夠精準，不論底層的 AI 模型如何更迭，都能穩定且安全地打造出符合檢驗業務高標準的現代化系統。

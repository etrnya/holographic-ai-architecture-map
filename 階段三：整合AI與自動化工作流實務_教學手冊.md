# 階段三：整合 AI 與自動化工作流實務

## 適用對象
具備基礎架構觀念，並希望將日常繁雜業務（如：報表擷取、檢驗標準比對、跨系統通知）自動化，同時導入 AI 輔助判讀的實踐者。

## 核心觀念：賦予系統「大腦」與「自動化生產線」
- **自動化輸送帶 (n8n)**：將程式碼轉化為視覺化的流程圖。甚麼時候觸發、資料往哪送，一目了然。
- **專屬知識庫 (RAG 架構)**：利用向量資料庫，先檢索企業內部真實文件，再讓 AI 回答，徹底消滅 AI 的「幻覺（胡說八道）」。

---

## 實作演練：由簡入深的三階段任務 (以 n8n 為例)

此階段建議在本機或雲端安裝免費的開源工具 **n8n** 進行實作。

### 難度一（簡單）：建立基礎 Webhook 觸發器
**目標**：熟悉視覺化介面，建立第一個能接收外部請求的工作流。

**實作步驟**：
1. 打開 n8n，建立一個新的 Workflow。
2. 新增一個 **Webhook Node**。設定 Method 為 `GET`，並複製畫面上顯示的 `Test URL`。
3. 新增一個 **Respond to Webhook Node**，連接在後方。設定回應內容為 JSON：`{"message": "自動化流程已啟動！"}`。
4. 點擊 `Execute Workflow`，然後在瀏覽器中貼上剛剛複製的網址。
5. **驗證**：瀏覽器畫面應顯示自訂的 JSON 訊息，代表成功打通了第一條自動化管線。

### 難度二（中階）：排程呼叫外部 API 與資料寫入
**目標**：不需人工干預，讓系統定時自動執行任務（例如每天早上抓資料）。

**實作步驟**：
1. 將原本的 Webhook 節點刪除，替換為 **Schedule Trigger Node**（設定為每天早上 09:00 觸發，或設定為每分鐘觸發以利測試）。
2. 新增一個 **HTTP Request Node**。設定網址為某個公開的政府資料 API（或天氣 API）。
3. 新增一個 **Google Sheets Node**。綁定帳號後，設定動作為 `Append Row`（新增一筆資料）。將上一步抓到的資料欄位，拖拉對應到試算表的欄位中。
4. **驗證**：啟動工作流，觀察試算表是否會自動長出新的資料。這取代了過去需要寫一長串 GAS 才能做到的事。

### 難度三（進階）：打造 AI 報表讀取工作流 (RAG 雛形)
**目標**：結合 AI 處理非結構化資料，將「死板的自動化」升級為「智慧工作流」。

**實作步驟**：
1. 建立一個觸發器（例如收到一封包含檢驗報表 PDF 的 Email，或 Webhook 接收檔案）。
2. 加入 **AI Agent Node (或 OpenAI/Gemini Node)**。
3. 在 AI 節點中，輸入強大的指令 (Prompt)：
   > 「請讀取傳入的文件內容。你是一位嚴謹的醫檢師。請提取出文件中的『檢驗批號』與『大腸桿菌數值』，並嚴格按照 JSON 格式輸出，不要包含任何其他廢話。」
4. 加入一個 **If Node (條件判斷)**。設定條件：如果提取出的數值大於法規標準，走 A 路線；小於等於標準，走 B 路線。
5. 針對異常的 A 路線，加入 **Line/Slack Notification Node**，發送緊急通知給主管。
6. 針對正常的 B 路線，寫入資料庫歸檔。

---
## 結語與反思
在難度三中，最關鍵的原則是 **「人在迴圈內 (Human in the loop)」**。AI 負責初判與整理（自動發通知或歸檔），但如果是高風險的醫療數據，最後的「核發報告」按鈕，仍需由專業醫檢師在前端網頁中親自按下。自動化是為了加速流程，而非推卸專業責任。

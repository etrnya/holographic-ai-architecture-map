# 階段四：系統上線部署與資訊安全實務

## 適用對象
準備將本機開發的測試系統，正式上線開放給團隊或外部單位使用的實踐者。這是處理敏感資料時最不可妥協的防線。

## 核心觀念：取得餐廳的「營運執照」與「安檢合格」
在自己電腦上跑通系統，只代表菜煮得出來。若要對外營業，必須處理：
1. **部署**：店面位置在哪裡？（讓別人能透過網址連進來）
2. **金鑰管理**：保險箱密碼藏在哪？（保護環境變數）
3. **權限控管**：誰有資格看帳本？（資安與存取控制）

---

## 實作演練：由簡入深的三階段任務

### 難度一（簡單）：將靜態網頁免費上線 (Vercel)
**目標**：將階段一寫的純前端網頁放上網路，讓世界上所有人都能透過網址存取。

**實作步驟**：
1. 註冊 **Vercel** 雲端平台帳號。
2. 在電腦上建立一個新資料夾，將階段一寫好的 `index.html` 放進去。
3. 登入 Vercel 後台，選擇 `Add New -> Project`。
4. Vercel 提供極度友善的介面，甚至可以直接將資料夾「拖曳 (Drag & Drop)」到指定區塊中上傳。
5. 點擊 Deploy，等待 30 秒。
6. **驗證**：Vercel 會配發一組 `.vercel.app` 的網址，將這組網址傳給手機或其他電腦測試，若能看到畫面即代表上線成功。

### 難度二（中階）：環境變數的隱身術
**目標**：把寫死在 `index.html` 裡面的 Supabase Key 隱藏起來。直接暴露 Key 是資安大忌。

**實作步驟**：
1. 如果使用的是階段二的 React 或 FastAPI 專案，在專案根目錄建立一個名為 `.env` 的文字檔。
   ```text
   SUPABASE_URL="你的真實網址"
   SUPABASE_KEY="你的真實密碼"
   ```
2. 將程式碼中的 `const URL = '...'` 改為讀取環境變數的語法（例如 Python 的 `os.getenv('SUPABASE_URL')` 或 Vite React 的 `import.meta.env.VITE_SUPABASE_URL`）。
3. **上傳部署時**：絕對不要把 `.env` 檔上傳。
4. **雲端設定**：進入 Vercel 或 Render 的後台控制面板，找到 `Environment Variables` (環境變數) 設定區塊，將上述兩個變數手動新增進去。平台會在編譯時自動把它們注入系統中，安全又隱密。

### 難度三（進階）：實作登入機制與資料列安全 (RLS)
**目標**：在檢驗機構中，資料存取必須遵循最小權限原則。確保登入的人只能看到他該看的資料。

**實作步驟**：
1. **啟用 Auth**：進入 Supabase 後台的 `Authentication` 模組，開啟 Email 登入功能，並手動建立一個測試員工帳號。
2. **設定 RLS 政策 (Row Level Security)**：
   - 進入 Database 介面，針對 `records` 資料表點選 `Enable RLS`。
   - 新增一條政策 (Policy)：`Enable read access for authenticated users only`。這段 SQL 底層邏輯意味著「沒有登入權杖 (Token) 的請求，資料庫將直接拒絕」。
3. **前端修改**：
   - 使用 Supabase 提供的 JavaScript 工具庫實作登入頁面。
   - 登入成功後，系統會發給瀏覽器一張「通行證 (JWT Token)」。
   - 在呼叫 API (`fetch`) 時，必須在 Headers 夾帶這張通行證。
4. **驗證**：在未登入狀態下呼叫資料庫，應收到 `401 Unauthorized` 錯誤；登入後才能正常看到檢驗報告。這達成了企業級的底層安全防護。

---
## 結語與反思
資安的黃金法則是：**前端的防禦都是防君子不防小人**。網頁上的任何限制（如隱藏按鈕、檢查格式）都可以被駭客輕易繞過。真正的安全檢查，包含權限驗證與資料格式檢查，必須 100% 在後端伺服器與資料庫（如 RLS）中重新驗證一次。掌握這個觀念，就能打造出合規且堅固的企業級應用。

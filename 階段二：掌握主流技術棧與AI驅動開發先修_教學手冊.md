# 階段二：掌握主流技術棧與 AI 驅動開發先修

## 適用對象
適合已理解「前後端 API 對接」原理（完成階段一），並準備進階至企業級標準架構的學習者。

## 核心觀念：從「代工廚房」升級為「專屬智慧廚房」
在階段一使用的 BaaS (Supabase) 適合快速驗證。但當業務邏輯變複雜（例如：在上傳資料前需要經過 OCR 圖片辨識或呼叫 AI 模型），就需要建立「專屬後端（自營廚房）」。
- **前端升級**：React。這是目前 AI 寫程式工具支援度最高、生態最完整的框架。
- **後端升級**：Python (FastAPI)。Python 是 AI 與資料處理的絕對王者。

在階段二，重點不是手寫每一行程式，而是**「學習如何指揮 AI 工具（如 Cursor）寫出框架代碼，並看懂它」**。

---

## 實作演練：由簡入深的三階段任務

### 難度一（簡單）：請 AI 蓋出專屬廚房 (FastAPI)
**目標**：不須熟記 Python 語法，透過精準的規格書 (Prompt) 讓 AI 生成一支能運作的 API。

**實作步驟**：
1. 開啟 AI 工具（如 ChatGPT 或 Cursor）。
2. 輸入以下 Prompt：
   > 「你是一個資深 Python 後端工程師。請幫我用 FastAPI 寫一支 API。這支 API 會接收一個 POST 請求，裡面包含一段文字 `text`。請將這段文字全部轉為大寫，並回傳 JSON 格式 `{"result": 大寫文字}`。請提供完整程式碼與如何在終端機執行的指令。」
3. **驗證與學習**：觀察 AI 產出的程式碼，找出「路由 (定義網址的地方)」與「邏輯 (轉換大寫的地方)」。將程式碼存為 `main.py` 並照著 AI 的提示執行。

### 難度二（中階）：請 AI 打造系統化外場 (React)
**目標**：透過 AI 生成 React 組件，理解現代前端的「組件化」思維。

**實作步驟**：
1. 繼續對 AI 下達指令：
   > 「現在請切換為前端工程師角色。請幫我寫一個簡單的 React 組件。畫面上要有一個文字輸入框和一個按鈕。這是一個獨立的組件 (Component)，不用完整的專案結構，請提供這個 `App.jsx` 的程式碼即可。」
2. **驗證與學習**：觀察程式碼中的 `useState`。這就是 React 紀錄「畫面狀態」的核心魔法，當狀態改變時，畫面會自動更新。

### 難度三（進階）：前後端完整對接與資料清洗
**目標**：將難度一與難度二結合，模擬真實業務場景中的前後端資料傳遞。

**實作步驟**：
1. 針對前端 React 代碼，下達進階指令：
   > 「請修改剛剛的 React 組件。當我點擊按鈕時，請使用 `fetch` 呼叫本機的 FastAPI `http://localhost:8000/api`。將輸入框的文字送過去，並把後端回傳的『大寫文字』顯示在按鈕下方。」
2. 啟動後端 FastAPI，並透過 React 網頁進行測試。
3. **進階挑戰**：要求 AI 在後端加上「資料清洗」邏輯。
   > Prompt：「請修改 FastAPI，如果在傳入的文字中發現『髒話』或『特殊符號』，請直接回傳錯誤代碼 400 與錯誤訊息，不要進行大寫轉換。」

---
## 結語與反思
透過上述演練可以發現，在 AI 時代，最困難的不是寫出 `fetch` 或 `FastAPI` 的語法，而是**「清楚定義輸入與輸出」**。當能把資料流（前端送什麼、後端回什麼）定義清楚，AI 就能在幾秒鐘內幫忙把程式碼建構完畢。這就是「AI 驅動架構師」的核心價值。
